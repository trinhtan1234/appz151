import 'package:flutter/material.dart';

class KiemTraHuHong extends StatelessWidget {
  const KiemTraHuHong({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Kiểm tra hư hỏng'),
      ),
      body: const Center(
        child: Text('Đang cập nhật'),
      ),
    );
  }
}
