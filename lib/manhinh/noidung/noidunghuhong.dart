import 'package:flutter/material.dart';

class NoiDungHuHong extends StatelessWidget {
  const NoiDungHuHong({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Nội dung sửa chữa hư hỏng'),
      ),
      body: const Center(
        child: Text('Đang cập nhật'),
      ),
    );
  }
}
